import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    input: {
        border: "none",
        borderRadius: "25px",
        padding: "5px 5px",
        fontColor: "#1A1A1A",
    },
});